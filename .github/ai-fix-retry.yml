name: AI Fix Build Errors

on:
  status:

jobs:
  ai-retry:
    uses: ao-org/.github/workflows/ai-fix-retry-reusable.yml@master
    secrets:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      JENKINS_USER: ${{ secrets.JENKINS_USER }}
      JENKINS_TOKEN: ${{ secrets.JENKINS_TOKEN }}
